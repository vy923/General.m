

function names = getHandles(s,typ)

fn = fieldnames(s);
a = [];
for i = 1:numel(fn)
    a.(getHandles())
    %fni = string(fn(i));
    if isobject(field)
        getHandles(s,typ);
        continue
    else 
        %disp(field)
    %elseif endsWith(class(field),typ)
    %    names(end+1) = field;
    end
end